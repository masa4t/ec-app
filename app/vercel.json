{
    "build": {
      "env": {
        "DATABASE_URL": "postgresql://postgres:Mgyahman420@localhost:5432/EC_DATA?schema=public"
      }
    },
    "buildCommand": "npm run build && npx prisma generate"
  }